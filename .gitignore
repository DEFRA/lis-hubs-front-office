.DS_Store
.npm/
.vscode
.idea
.public
node_modules
npm-debug.log
coverage
.cache
.envrc
tsconfig.tsbuildinfo
.env
.npmrc

# TODO: remove once @livestock deps are published to a proper registry.
# Ignored for now because local lockfiles resolve @livestock from the local
# Verdaccio (verdaccio.lis.defra), which CI/CDP cannot reach.
package-lock.json
